services:
  h2:
    hostname: 'myapp'
    container_name: mycontainername
    image: mastilo/anotherimage
    ports:
      - 8080:8080
    volumes:
      - h2-data:/opt/h2-data
volumes:
  h2-data:


 ///mysql or some shit

 version: '3'

 services:
   docker-mysql:
     image: mysql:latest
     environment:
       - MYSQL_ROOT_PASSWORD=root
       - MYSQL_DATABASE=demodb
       - MYSQL_USER=root
       - MYSQL_PASSWORD=root
     ports:
       - 3307:3306

   app:
     image: mastilo/anotherimage:latest
     ports:
       - 8080:8080
     environment:
       SPRING_DATASOURCE_URL: jdbc:mysql://docker-mysql:3306/database?autoReconnect=true&useSSL=false
     depends_on:
       - mysql